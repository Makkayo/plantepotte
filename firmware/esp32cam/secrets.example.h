// secrets.example.h — MAL for secrets.h.
// Kopier denne fila til `secrets.h` (samme mappe) og fyll inn WiFi-navn og
// passord. `secrets.h` er gitignored — passordet ditt havner ALDRI på GitHub.
// (Samme mønster som firmware/config.example.py -> config.py.)

#pragma once

const char* WIFI_SSID = "DITT-WIFI-NAVN";       // kun 2,4 GHz
const char* WIFI_PASS = "DITT-WIFI-PASSORD";
